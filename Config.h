#define CLOCK_MULTIPLIER (100)
#define TICK_MICROS (100)
#define MCU_LIMIT (5)
#define CODE_OVERHEAD_PENALTY (100)
#define AIR_DRAG_COEF (0.024)
#define AIR_DENSITY (0.45)

#define DEBUG_OUT cout
// #define DEBUG_OUT if (0) cout

// #define VERBOSE_OUT cout
#define VERBOSE_OUT if (0) cout
